name: Osibusi Biraz IPTV Otomatik

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:

jobs:
  run-osibusi:
    runs-on: ubuntu-latest
    steps:
      - name: Repo'yu Klonla
        uses: actions/checkout@v4

      - name: Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Bağımlılıkları Kur
        run: |
          pip install --upgrade pip
          pip install requests httpx

      - name: Script Çalıştır
        run: |
          echo '🚀 biraz.py çalıştırılıyor...'
          python biraz.py
          echo '✅ Script tamamlandı.'
          ls -R M3U || echo '⚠️ M3U klasörü boş olabilir.'

      - name: Git İşlemleri
        shell: bash
        run: |
          echo '🔧 Git yapılandırması yapılıyor...'
          git config --global user.name 'github-acti
