name: Osibusi biraz IPTV Otomatik

on:
  schedule:
    - cron: "0 */3 * * *" # Her 3 saatte bir
  workflow_dispatch:

jobs:
  run-osibusi:
    runs-on: ubuntu-latest
    steps:
      - name: Repo'yu Klonla
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Bağımlılıkları Kur
        run: |
          pip install requests httpx

      - name: OSIBUSI Script Çalıştır
        run: |
          python biraz.py

      - name: Değişiklikleri Git'e Ekle ve Push Et
        shell: bash
        run: |
          echo "🔧 Git yapılandırması yapılıyor..."
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          echo "💾 Değişiklikler hazırlanıyor..."
          git add Osibusibiraz.m3u || true

          echo "🧩 Pull & Rebase yapılıyor..."
          git stash || true
          git pull --rebase origin main || true
          git stash pop || true

          echo "🪶 Commit işlemi..."
          COMMIT_MESSAGE="Otomatik güncelleme: $(date "+%Y-%m-%d %H:%M:%S")"
          git commit -m "$COMMIT_MESSAGE" || echo "Değişiklik yok"

          echo "🚀 Push işlemi..."
          git push origin main || echo "Push başarısız, lütfen manuel kontrol et."
